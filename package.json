{
  "name": "sdui-mock-backend",
  "version": "1.0.0",
  "description": "Mock SDUI backend serving screen JSON files with manifest-based cache invalidation",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4",
    "cors": "^2",
    "chokidar": "^3"
  },
  "devDependencies": {
    "nodemon": "^3"
  }
}
